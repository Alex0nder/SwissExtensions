# Tab Memory Cleaner

Discard background tabs to free memory. One click.

**Install:** Chrome → `chrome://extensions` → Developer mode → Load unpacked → select `TabMemoryCleaner` folder.

- Discard all background tabs (active tab kept)
- Optional: skip pinned tabs
- Gradual discard to avoid browser freeze
